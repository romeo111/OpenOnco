_contribution:
  chunk_id: source-recency-refresh-wave2-2026-05-01-009
  contributor: claude-anthropic-internal
  submission_date: '2026-05-01'
  ai_tool: claude-code
  ai_model: claude-opus-4-7
  ai_model_version: 1m-context
  ai_session_notes: 'Wave 2 batch — closes #211. Real HTTP GET checks on 5 sources
    from chunk manifest via httpx with User-Agent: "Mozilla/5.0 (compatible;
    OpenOnco-recency-bot)", 30s timeout, up to 3 retries. Pattern mirrors the
    original source-recency-refresh chunks (#92-#121). Per-source upsert touches
    only: current_as_of, last_recency_check, recency_check_status_code, and
    (where the primary URL was 403/404) the url field is replaced with a
    publisher landing page that returned 200. Content / license / attribution
    fields unchanged.'
  tasktorrent_version: 2026-05-01-wave2
  notes_for_reviewer: 'EANO-LGG: PubMed 403 on bot UA, DOI redirects to
    Nature article and returns 200 — proposing URL replacement. NCCN-BONE-SARCOMA:
    stub source with no URL on file — unreachable + suggested NCCN guideline
    landing page. COMFORT-I / KEYNOTE-024 / BLAST: publisher 403 on bot UA
    (NEJM, ASH) for both primary and DOI fallback — flagged as
    unreachable.blocked, no upsert proposed (real-browser check by maintainer
    likely succeeds).'
