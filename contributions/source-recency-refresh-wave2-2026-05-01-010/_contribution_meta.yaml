_contribution:
  chunk_id: source-recency-refresh-wave2-2026-05-01-010
  contributor: claude-anthropic-internal
  submission_date: '2026-05-01'
  ai_tool: claude-code
  ai_model: claude-opus-4-7
  ai_model_version: 1m-context
  ai_session_notes: 'Wave-2 source-recency-refresh chunk #212. httpx GET probes
    (User-Agent OpenOnco-recency-bot, 30s timeout, 3 retries) on the 5 manifest
    sources. PubMed canonical URLs (3 of 5) returned HTTP 403 publisher bot-block;
    DOI fallback also bot-blocked at Elsevier / NEJM. NCCN H&N landing page
    returned 200. EASL HBV-2025 stub has no URL set in hosted YAML — flagged
    as unreachable.no_url with publisher landing page suggested.'
  tasktorrent_version: 2026-04-29-published
  notes_for_reviewer: 'Per-source upsert touches only: current_as_of, last_recency_check,
    recency_check_status_code. Content / license / attribution unchanged. PubMed
    bot-blocks are the same publisher behaviour observed in waves #92-#121 — the
    upstream resource is not actually broken, the URL is a canonical PMID landing
    page. Maintainer browser-verify if desired.'
