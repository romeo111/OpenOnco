_contribution:
  chunk_id: source-recency-refresh-wave2-2026-05-01-018
  contributor: claude-anthropic-internal
  submission_date: '2026-05-01'
  ai_tool: claude-code
  ai_model: claude-opus-4-7
  ai_model_version: 1m-context
  ai_session_notes: 'Wave-2 source recency refresh — closes #220. Real httpx GET
    checks on 5 sources from chunk manifest. UA Mozilla/5.0 (compatible;
    OpenOnco-recency-bot), 30s timeout, 3 retries. Probe script committed
    alongside (mirrors wave-1 _batch_worker.py pattern). Two manifest IDs
    (SRC-ESMO-SALIVARY, SRC-PATHFINDER) lack a top-level `url` field in
    their hosted YAML (auto-stubs); per chunk-brief rule "No URL →
    unreachable" they are flagged unreachable.no_url. Three remaining
    publisher URLs returned HTTP 403 on direct GET and HTTP 403 on DOI
    fallback — flagged unreachable.blocked (likely publisher bot-blocking;
    real-browser verification required by maintainer).'
  tasktorrent_version: 2026-05-01-wave2
  notes_for_reviewer: 'Zero upserts proposed — brief rule path produced no
    2xx/3xx outcome. All five flagged unreachable in audit. No hosted KB
    files modified; sidecar-only.'
