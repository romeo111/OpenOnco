_contribution:
  chunk_id: source-recency-refresh-wave2-2026-05-01-022
  contributor: claude-anthropic-internal
  submission_date: '2026-05-01'
  ai_tool: claude-code
  ai_model: claude-opus-4-7
  ai_model_version: 1m-context
  ai_session_notes: 'Wave-2 source recency refresh — closes #224. Real httpx HEAD
    checks on 5 sources from chunk manifest. UA Mozilla/5.0 (compatible;
    OpenOnco-recency-bot), 30s timeout, 3 retries, follow_redirects=True.
    Probe script committed alongside (_recency_probe.py). Two manifest IDs
    (SRC-FIGHT, SRC-VOYAGER) lack a top-level url field in their hosted
    YAML (auto-stubs awaiting clinical signoff); per chunk-brief rule
    "No URL → unreachable.no_url" they are flagged accordingly. Three
    publisher URLs returned HTTP 403 on primary HEAD; brief 403 ladder
    applied (DOI HEAD fallback, then PubMed canonical HEAD fallback) and
    all three layers returned 403 — flagged unreachable.blocked (likely
    publisher bot-blocking; real-browser verification required by
    maintainer).'
  tasktorrent_version: 2026-05-01-wave2
  notes_for_reviewer: 'Zero upserts proposed — brief rule path produced no
    2xx/3xx outcome on any of the five manifest IDs. All five flagged
    unreachable in audit. No hosted KB files modified; sidecar-only.'
